# Stock Candles Web Client (Optional)

Vue 3 + Vite frontend scaffold. Business logic and API integration are not implemented yet.

## Setup

```bash
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8080`.
